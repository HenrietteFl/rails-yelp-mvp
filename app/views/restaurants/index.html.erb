<h1>Restaurants</h1>
<% @restaurants.each do |restaurant| %>
<p><%= restaurant.name %> <%= link_to 'Details', restaurant_path(restaurant) %></p>
<% end %>

<p>
<%= link_to 'Add a new restaurant', new_restaurant_path() %>
</p>
